# Expense Tracker
a simple web app that tracks your expenses
## About
A web app built with Flask and SQLite that allows users to add, list, modify, delete, and summarize their expenses through a browser interface.
## Usage
1. Clone the repository
2. Create a virtual environment: python -m venv venv
3. Activate it: venv\Scripts\activate (Windows) or source venv/bin/activate (Mac/Linux)
4. Install dependencies: pip install -r requirements.txt
5. Run the app: python app.py
6. Open your browser at: http://127.0.0.1:5000
## Upcoming Features
- Improve the UI by using modern UI design 
- A whole refactor for the backend add some new features and remove the usless one 
## Project Structure
```
ExpenseTracker/
├── static/
├── templates/
├── .gitignore
├── data.db
├── app.py
├── requirements.txt
├── tracker.py
└── README.md
```